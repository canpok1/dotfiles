---
paths:
  - "**/*.go"
  - "**/*.{ts,tsx}"
  - "**/*.{js,jsx,mjs,cjs}"
  - "**/*.{sh,bash}"
---

# 開発ルール

コードを変更する作業に適用する。

## 実装後の品質確認

実装したら、次の2点を行うこと。該当する目的のスキル／コマンドが利用可能であれば積極的に活用する。

- コードの品質を確認・改善する。
- 自分が書いたコードをレビューし、指摘があれば修正する。

## コミット

- コミットする際は、論理的な変更ごとに分けること。
